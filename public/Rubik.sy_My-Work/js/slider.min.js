console.clear();const elPrevButton=document.querySelector("#prev"),elNextButton=document.querySelector("#next"),flipping=new Flipping,elImages=Array.from(document.querySelectorAll(".ui-big-image")),elThumbnails=Array.from(document.querySelectorAll(".ui-thumbnail"));let state={photo:0};function send(e){flipping.read();const t=document.querySelectorAll("[data-active]");switch(Array.from(t).forEach(e=>e.removeAttribute("data-active")),e){case"PREV":state.photo--;break;case"NEXT":state.photo++;break;default:state.photo=+e}var a=elImages.length;state.photo=Math.max(0,Math.min(state.photo,a-1)),Array.from(document.querySelectorAll(`[data-key="${state.photo}"]`)).forEach(e=>{e.setAttribute("data-active",!0)}),flipping.flip()}elThumbnails.forEach(e=>{e.addEventListener("click",()=>{send(e.dataset.key)})}),elPrevButton.addEventListener("click",()=>{send("PREV")}),elNextButton.addEventListener("click",()=>{send("NEXT")}),send(0);